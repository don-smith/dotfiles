#!/bin/zsh

gh() {
  ssh-add ~/.ssh/github
}

bb() {
  ssh-add ~/.ssh/bitbucket
}

gitlab() {
  ssh-add ~/.ssh/gitlab
}

digo() {
  ssh-add ~/.ssh/digital_ocean
}

ssbpub() {
  ssh-add ~/.ssh/ssb_pub
}

